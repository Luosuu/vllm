# Ralph Progress Log
Started: 2026-03-10
---

## Codebase Patterns
- Profiler code lives in `vllm/profiler/wrapper.py` (implementations) and `vllm/config/profiler.py` (config)
- `WorkerProfiler` is the abstract base class; subclasses implement `_start()`, `_stop()`, and `annotate_context_manager()`
- Profiler creation is inline in GPU worker's `profile()` method (no separate factory function)
- GPU worker uses lazy profiler init; CPU/XPU workers use eager init
- `triton.profiler` is the import path for Proton (`import triton.profiler as proton`)
- Proton API: `proton.start(name, hook)` returns session_id, `proton.finalize(session=id)`, `proton.scope(name)` is a context manager
- `proton.start()` accepts: name, context, data, backend, mode, hook — all keyword-only except name
- ProtonProfilerWrapper takes `output_dir` and `local_rank` params (not `worker_name` like Torch)
- In periodic mode: `_create_session()` on first `_start()`, then `activate()`/`deactivate(flushing=True)` on subsequent cycles; `finalize()` only on `shutdown()`
- In non-periodic mode: `_create_session()` + `finalize()` on every start/stop cycle
- Start/stop profile API is pass-through: LLM → LLMEngine → EngineCore → Worker.profile() — only worker has profiler type branching
- GPU worker has TWO places where profiler type matters: (1) early validation in `__init__` and (2) lazy instantiation in `profile()` — both must be kept in sync
- Environment managed by `uv`; activate with `. .venv/bin/activate`
- Cannot mock triton.profiler via sys.modules when it's already loaded — instead patch wrapper._proton after construction
- Use _make_wrapper_with_mock() helper pattern: construct with real proton, then replace _proton attribute with MagicMock
- CUDA graph capture happens in `compile_or_warm_up_model()` at `self.model_runner.capture_model()` (gpu_worker.py line 522)
- `compile_or_warm_up_model()` sequence: warmup runs → kernel_warmup → capture_model()
- Early-start pattern: `start_and_deactivate()` → GPU activity → `deactivate_early()` → normal `start()`/`stop()` reuses session via `activate()`
- `_was_activated` flag tracks whether current cycle was started via `activate()` (affects `_stop()` behavior in non-periodic mode)
- Early Proton start is wired in `compile_or_warm_up_model()` between `kernel_warmup()` and `capture_model()` — profiler stored in `self.profiler` for reuse by `profile()`
---

## 2026-03-10 - US-001
- Implemented `start_and_deactivate()` and `deactivate_early()` public methods on ProtonProfilerWrapper
- Modified `_start()` to detect existing `_session_id` and call `proton.activate()` instead of `proton.start()` (both modes)
- Modified `_stop()` in non-periodic mode to call `deactivate(flushing=True)` before `finalize()` when session was activated
- Added `_was_activated` flag to track activate vs start lifecycle
- Added 8 unit tests in TestProtonEarlyStart class covering both modes, regression, and full lifecycle
- Files changed: `vllm/profiler/wrapper.py`, `tests/v1/worker/test_proton_profiler.py`
- **Learnings for future iterations:**
  - The `_start()` refactor unified periodic and non-periodic session reuse logic — both now check `_session_id is not None` to decide activate vs start
  - `_was_activated` flag is essential for non-periodic `_stop()` — without it, finalize alone would miss flushing profiling data
  - All 68 existing + new tests pass; no regressions in periodic or non-periodic modes
---

## 2026-03-10 - US-002
- Wired early Proton start in `compile_or_warm_up_model()`: creates ProtonProfilerWrapper and calls `start_and_deactivate()` before `capture_model()`, then `deactivate_early()` after capture
- Moved `ProtonProfilerWrapper` import from lazy (inside `profile()`) to top-level import in gpu_worker.py
- Added 7 unit tests in TestProtonEarlyStartInWorker class covering: wrapper creation, deactivation after capture, session reuse via profile(), torch/cuda unaffected, enforce_eager mode, no-profiler-config case
- Files changed: `vllm/v1/worker/gpu_worker.py`, `tests/v1/worker/test_proton_profiler.py`
- **Learnings for future iterations:**
  - The deactivation guard uses `isinstance(self.profiler, ProtonProfilerWrapper)` + `_session_id is not None` + `not _running` to safely detect early-started state
  - Early start is placed after `kernel_warmup()` but before `capture_model()` — this is the correct position because kernel warmup doesn't involve CUDA graphs
  - The `profile()` method's `if self.profiler is None` guard at line 796 naturally skips re-creation when the profiler was early-initialized
  - All 75 tests pass (68 existing + 7 new); no typecheck regressions
---
