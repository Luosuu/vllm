# Ralph Progress Log
Started: Wed Mar  5 00:00:00 UTC 2026

## Codebase Patterns
- Profiler code lives in `vllm/profiler/wrapper.py` (implementations) and `vllm/config/profiler.py` (config)
- `ProfilerKind` is a `Literal` type in `vllm/config/profiler.py`
- `WorkerProfiler` is the abstract base class; subclasses implement `_start()`, `_stop()`, and `annotate_context_manager()`
- Profiler creation is inline in GPU worker's `profile()` method (no separate factory function)
- GPU worker uses lazy profiler init; CPU/XPU workers use eager init
- `triton.profiler` is the import path for Proton (`import triton.profiler as proton`)
- Proton API: `proton.start(name, hook)` returns session_id, `proton.finalize(session=id)`, `proton.scope(name)` is a context manager
- `proton.start()` accepts: name, context ("shadow"/"python"), data ("tree"/"trace"), backend ("cupti"/"roctracer"/"instrumentation"/None), mode (string), hook ("triton"/None)
- ProtonProfilerWrapper takes `output_dir` and `local_rank` params (not `worker_name` like Torch)
- `torch_profiler_dir` is for torch profiler only; `proton_profiler_dir` is for proton profiler (fully separated)
- Proton validation lives in `_validate_proton_fields()` helper called from main validator
- Periodic flushing mode detected by checking `proton_mode.startswith("periodic_flushing")` — mode string may be "periodic_flushing" or "periodic_flushing:format=hatchet_msgpack"
- In periodic mode: `_create_session()` on first `_start()`, then `activate()`/`deactivate(flushing=True)` on subsequent cycles; `finalize()` only on `shutdown()`
- In non-periodic mode: `_create_session()` + `finalize()` on every start/stop cycle (unchanged from before)
- Start/stop profile API is pass-through: LLM → LLMEngine → EngineCore → Worker.profile() — only worker has profiler type branching
- GPU worker has TWO places where profiler type matters: (1) early validation in `__init__` and (2) lazy instantiation in `profile()` — both must be kept in sync
- ProtonProfilerWrapper.get_status() returns a plain dict — use this for API endpoint responses
- Environment managed by `uv`; activate with `. .venv/bin/activate`
- For TP>1, use `--tensor-parallel-size 2` with `vllm serve`; each rank produces its own output file
- Proton source and docs at `submodules/triton/third_party/proton`
---

## 2026-03-05 - US-001
- Added 6 Proton-specific config fields to ProfilerConfig in vllm/config/profiler.py
- Fields: proton_profiler_dir, proton_context, proton_data, proton_backend, proton_mode, proton_hook
- All fields have docstrings explaining valid values and defaults
- Files changed: vllm/config/profiler.py
- **Learnings for future iterations:**
  - The proton fields follow the same naming pattern as torch_profiler_* but with proton_ prefix
  - All existing torch/cuda fields are unchanged — new fields were inserted between torch_profiler_dir and torch_profiler_with_stack
  - Typecheck and all 22 existing profiler tests pass without changes
---

## 2026-03-05 - US-002
- Added validation for all Proton config fields in `_validate_proton_fields()` method
- proton_profiler_dir required when profiler='proton', converted to absolute path
- proton_context validated to 'shadow'/'python'
- proton_data validated to 'tree'/'trace'
- proton_backend validated to 'cupti'/'roctracer'/'instrumentation'/None
- proton_hook validated to 'triton'/None (proton_mode accepts any string)
- Updated GPU worker to use `proton_profiler_dir` instead of `torch_profiler_dir` for proton output
- Updated all existing tests to pass `proton_profiler_dir` to config
- Files changed: vllm/config/profiler.py, vllm/v1/worker/gpu_worker.py, tests/v1/worker/test_proton_profiler.py
- **Learnings for future iterations:**
  - Validation is split into main validator + `_validate_proton_fields()` helper for clarity
  - GPU worker's `profile()` method at line ~788 references config fields — must be updated when config fields change
  - Existing tests that create `ProfilerConfig(profiler="proton")` all need `proton_profiler_dir` now
  - The `torch_profiler_dir` field was previously shared with proton — now fully separated
---

## 2026-03-05 - US-003
- Wired all 6 Proton config fields into ProtonProfilerWrapper
- `__init__()` now reads proton_context, proton_data, proton_backend, proton_mode, proton_hook from ProfilerConfig
- `_start()` now passes all config fields to `proton.start()` instead of hardcoding `hook="triton"`
- Output file path still uses `proton_profiler_dir` with rank suffix
- Added comprehensive docstrings and inline comments for parameter mapping
- Files changed: vllm/profiler/wrapper.py
- **Learnings for future iterations:**
  - ProtonProfilerWrapper stores config fields as `self._context`, `self._data`, etc. (private attributes)
  - `proton.start()` signature: `name, *, context, data, backend, mode, hook` — all keyword-only except name
  - The wrapper constructor signature (output_dir, local_rank) is unchanged — GPU worker code didn't need changes
  - All 22 existing tests pass without modification since they use defaults matching the old hardcoded values
---

## 2026-03-05 - US-004
- Added 15 new tests for Proton config validation and wiring (37 total)
- Unit tests: invalid proton_context, proton_data, proton_backend, proton_hook all raise ValueError
- Unit tests: proton_mode accepts arbitrary strings, all valid enum values accepted
- Unit tests: relative proton_profiler_dir converted to absolute, URI paths preserved
- Unit test: valid config with all fields explicitly set
- Integration tests: config fields correctly forwarded to proton.start() arguments
- Integration test: default config values forwarded correctly
- Integration test: _stop() calls proton.finalize() with correct session ID
- Files changed: tests/v1/worker/test_proton_profiler.py
- **Learnings for future iterations:**
  - Cannot mock triton.profiler via sys.modules when it's already loaded — instead patch wrapper._proton after construction
  - Use _make_wrapper_with_mock() helper pattern: construct with real proton, then replace _proton attribute with MagicMock
  - proton.start() is called with keyword arguments — verify via call_args.kwargs
---

## 2026-03-05 - US-005
- Validated Proton config end-to-end with vllm serve on 2x L40S GPUs
- Test 1 (tree/shadow): `vllm serve openai/gpt-oss-20b --tensor-parallel-size 2 --profiler-config '{"profiler":"proton","proton_profiler_dir":"/tmp/proton_output","proton_context":"shadow","proton_data":"tree","proton_hook":"triton"}'` started without errors
- Triggered /start_profile, sent 3 chat completion requests, triggered /stop_profile
- Output: proton_rank0.hatchet (160B) and proton_rank1.hatchet (160B) — valid JSON tree format
- Test 2 (trace/shadow): Changed proton_data to "trace" — output: proton_rank0.chrome_trace (11MB) and proton_rank1.chrome_trace (11MB)
- Test 3 (tree/python): Changed proton_context to "python" — output: proton_rank0.hatchet (3.8MB) with rich Python call stacks, confirming python context captures Python-level profiling data
- No code changes needed — all functionality worked correctly with existing implementation
- Files changed: scripts/ralph/prd.json, scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - Shadow context produces minimal output (160B) with just ROOT node when using hook="triton" — this is expected since shadow mode only captures GPU kernel data
  - Python context produces much richer output (3.8MB) with full Python call stacks
  - Trace format (.chrome_trace) files are much larger than tree format (.hatchet) files
  - Server startup takes ~35-45s on 2x L40S GPUs with openai/gpt-oss-20b model
  - Chat completions endpoint works; completions endpoint may have JSON escape issues with certain shells
---

## 2026-03-05 - US-006
- Added periodic_flushing mode to ProtonProfilerWrapper
- Detects "periodic_flushing" prefix in proton_mode to switch lifecycle
- Extracted _create_session() helper from _start() for reuse in both modes
- Periodic mode: session created once on first _start(), reactivated via proton.activate() on subsequent calls
- Periodic _stop() calls proton.deactivate(flushing=True) instead of proton.finalize()
- Overrode shutdown() to call proton.finalize() only on shutdown in periodic mode
- Non-periodic mode continues to work exactly as before (start/finalize per cycle)
- All 37 existing tests pass without changes
- Typecheck (mypy) passes
- Files changed: vllm/profiler/wrapper.py
- **Learnings for future iterations:**
  - _create_session() encapsulates proton.start() with all config fields — reuse for both modes
  - In periodic mode, proton.start() is only called once (first _start()); it both creates and activates the session
  - shutdown() override must handle the case where profiler is still running (call _call_stop() first)
  - The _periodic_mode flag is set in __init__ and used to branch in _start(), _stop(), and shutdown()
---

## 2026-03-05 - US-007
- Added phase tracking to ProtonProfilerWrapper: _current_phase (int) and _output_files (list)
- Added _scan_output_files() to scan output directory for files matching this rank
- Added get_status() method returning dict with active, current_phase, output_dir, output_files
- Phase advances on each _stop() in periodic mode via proton.data.advance_phase(session)
- Added 13 new tests for phase tracking and get_status() (49 total)
- Files changed: vllm/profiler/wrapper.py, tests/v1/worker/test_proton_profiler.py
- **Learnings for future iterations:**
  - proton.data.advance_phase(session) is called after deactivate() to advance to next phase
  - _scan_output_files() filters by "proton_rank{N}" prefix to only include files for this rank
  - get_status() returns a plain dict — suitable for JSON serialization in API endpoints
  - Phase counter is local to the wrapper instance; each rank tracks its own phase independently
---
